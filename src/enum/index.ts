export const enum FontFileType {
    local = "local",
    remote = "remote"
}

// "画布宽高", "内容宽高", "逐行导出", "逐字导出"
export const enum ExportType {
    View = 1,
    ViewContent = 2,
    PerLine = 3,
    PerWord = 4

}
